# LWM - Legacy Window Manager

Простой и быстрый оконный менеджер для X11 на языке C.

## Требования

- Linux с поддержкой X11
- GCC компилятор
- Библиотека libX11 и её заголовки (libx11-dev или аналогичный пакет)

## Установка

### Клонирование и сборка

```bash
cd ~/WM
make
sudo make install
```

### Удаление

```bash
sudo make uninstall
```

## Запуск

Добавьте в ваш `~/.xinitrc`:

```bash
exec lwm
```

Или запустите напрямую:

```bash
lwm
```

## Конфигурация

Конфиг располагается в `~/.config/lwm.conf`

При первом запуске автоматически создаётся файл с параметрами по умолчанию.

### Доступные параметры

- `BAR_COLOR` - цвет панели
- `BG_COLOR` - цвет фона
- `BORDER_COLOR` - цвет границ окон
- `BUTTON_COLOR` - цвет кнопок
- `TEXT_COLOR` - цвет текста
- `LINE_COLOR` - цвет разделителей
- `FONT` - название шрифта (fixed, 6x13, 9x15, etc)

## Клавиатурные сокращения

| Комбинация | Действие |
|-----------|----------|
| `Super + Return` | Открыть терминал (xterm) |
| `Super + Tab` | Показать меню скрытых окон |
| `Alt + Tab` | Переключиться на другое окно |
| `Super + U` | Показать все скрытые окна |
| `Super + Q` | Выход из WM |
| `Super + C` | Закрыть активное окно |
| `Super + D` | Скриншот (flameshot) |

## Управление окнами

- **Мышь + Alt + ПКМ** - изменение размера окна
- **ЛКМ по заголовку** - перемещение окна
- **ПКМ на кнопке закрытия** - закрытие окна
- **ЛКМ на кнопке свёртывания** - свёртывание окна

## Особенности

- Поддержка EWMH (Extended Window Manager Hints)
- Информационная панель с временем и использованием ОЗУ
- Прозрачная обработка ошибок X11
- Автоматическое создание конфига

## Лицензия

MIT
